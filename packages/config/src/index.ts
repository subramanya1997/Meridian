export { env } from "./env";

export const APP_NAME = "Meridian Commerce";
export const APP_VERSION = "1.0.0-mvp";

export const DEFAULT_CURRENCY = "USD";
export const DEFAULT_LOCALE = "en-US";

export const PAGINATION = {
  DEFAULT_PAGE_SIZE: 25,
  MAX_PAGE_SIZE: 100,
} as const;
